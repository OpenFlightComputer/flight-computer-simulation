# Scenarios

Versioned JSON scenarios belong here. A scenario defines timing, initial state,
vehicle configuration, objective producer, inputs, deterministic random seed,
logging cadence, and any explicit faults.

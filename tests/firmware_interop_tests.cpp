#include "ofcsim/firmware_c.hpp"

#include <gtest/gtest.h>

TEST(FirmwareInterop, DefaultsPassFirmwareValidation)
{
    GTEST_SKIP() << "Learning task: construct the firmware defaults and validate them.";
}

TEST(FirmwareInterop, ControlCoreExecutesFromCpp)
{
    GTEST_SKIP() << "Learning task: reproduce one firmware control-core case from C++.";
}
